{
  "name": "project-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Project management portal — projects, tasks, quotes, people, documents",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mammoth": "^1.12.1",
    "multer": "^1.4.5-lts.1",
    "pdfkit": "^0.19.1"
  }
}
